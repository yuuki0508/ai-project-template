# __PROJECT_TITLE__

Claude Code / その他のAIエージェント向けの入口。

まず次の2つを読むこと。Cursorの場合は自動で読み込まれる。

- `.cursor/rules/00-project-context.mdc` — 案件の全体像と、情報の在り処
- `.cursor/rules/10-conventions.mdc` — 守るべきルール

そのうえで `.cursor/rules/` 配下の残りのファイルから、これから触る領域に該当するものを読む。

作業単位は `tasks/active/` の1ファイル。無ければ、作ってから始めるよう促すこと。
