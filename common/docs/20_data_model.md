# データモデル — __PROJECT_TITLE__

> **カラム定義の一次情報はマイグレーション。** ここにDDLを転記しない（必ず腐る）。
> このファイルには「テーブルの意味」「関連」「設計意図」だけを書く。

## テーブル一覧

| テーブル | 役割 | 主な関連 |
|---|---|---|
|  |  |  |

## 関連図

```
（テキストで足りる規模ならテキストで書く。ER図が必要になったら生成ツールを使う）

users --< orders --< order_items >-- items
```

## 各テーブルの設計意図

### `テーブル名`

- 何を1行として持つのか:
- 論理削除の扱い:
- 迷った点と結論（詳細は `docs/decisions/`）:

## 命名の約束

- テーブル名は複数形・スネークケース
- 日本語の業務用語との対応は `docs/glossary.md` を必ず参照する
- 金額は整数（最小単位）で保持する。浮動小数点は使わない
- 日時は UTC で保存し、表示時に変換する

## 状態遷移

状態を持つテーブルは、取りうる値と遷移をここに明記する。

```
下書き → 申請中 → 承認済 → 完了
            ↓
          却下
```
